@Html.AntiForgeryToken()
<div class="dropdown">
    <button class="btn btn-link text-white position-relative p-0" type="button" id="btnCampana" data-bs-toggle="dropdown" aria-expanded="false">
        <i class="bi bi-bell-fill c-notif-bell-icon"></i>
        <span id="badgeNoLeidas" class="badge rounded-pill bg-danger position-absolute top-0 start-100 translate-middle d-none c-notif-badge">
            0
        </span>
    </button>
    <div class="dropdown-menu dropdown-menu-end p-0 c-notif-dropdown">
        <div class="px-3 py-2 border-bottom fw-semibold c-notif-title">
            Notificaciones
        </div>
        <ul class="list-unstyled mb-0" id="listaNotificaciones">
            <li class="text-center text-muted small py-4">No hay notificaciones nuevas.</li>
        </ul>
    </div>
</div>